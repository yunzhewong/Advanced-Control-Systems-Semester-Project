close all
s = tf('s');

figure
rlocus(sys)

% with lead and integrator
base_lead_controller = zpk([-5 ], [-500], 1);
figure
rlocus(sys * base_lead_controller / s)

controller = zpk([-5 -10], [0 -100 -150],1e4);
[c_b, c_a] = tfdata(controller,'v');
